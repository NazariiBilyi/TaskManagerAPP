import './Header.css';

const Header = () => {
    return (
        <header className="header">
            <h1 className="header-title">Task Manager Program</h1>
        </header>
    );
};

export default Header;